{
  "name": "owl",
  "version": "1.0.0",
  "description": "rc lee hall booking system",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd react-app && npm install && npm run build && cp -r build/ ../express-app/build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rcltech/owl.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^3.0.8"
  },
  "husky": {
    "hooks": {
      "pre-commit": "echo \"build react and copy into express directory\" && npm run build && git add build"
    }
  }
}
